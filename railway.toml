[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd rag-deployment/backend && uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "backend"

